source 'https://rubygems.org'

# Specify your gem's dependencies in pupistry.gemspec
gemspec

# See comments in gemspec around Puppet - we need it to run via the bundle
# so we include it here, but we don't want it as a dependency of the gem
# itself.
gem "puppet"
gem "which"

